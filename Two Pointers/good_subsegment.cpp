#include <bits/stdc++.h>
using namespace std;

// Find the largest lenght of the subsegment such that the | max(S[l....r]) - min(S[l...r]) <= K |

int main(){
    cin.tie(0)->sync_with_stdio(0);
    int N;cin >> N;
    vector<int> A(N);for(int &a: A)cin >> a;



    return 0;
}