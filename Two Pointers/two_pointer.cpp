/* 
* Two Pointer Technique: Iterating two monotonic pointers 
* across an array to search for a pair of indices satisfying some condition in linear time.
* 
* Ex. A[i] + A[j] <= X
* Ex. A[i] + A[j] = X
*/

#include <bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(0)->sync_with_stdio(0);
    return 0;
}